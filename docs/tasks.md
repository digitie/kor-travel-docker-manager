# TASKS — 활성 작업

이 문서는 완료되지 않은 작업만 순서대로 한 줄씩 나열한다. lane, 담당자 구분,
계층형 하위 작업과 완료 이력은 두지 않는다. 완료 이력은
[`docs/tasks-done.md`](tasks-done.md), 실행 근거와 현재 상태는
[`docs/journal.md`](journal.md)가 정본이다.

- [/] M05 execution identity v6 — v5 Map·PinVi source pinset은 보존하고 trusted Manager revision을 포함한 v6 execution identity를 registry·`ktdctl`·one-shot ledger·terminal block·public generation binding에 연결하며, M05 provenance preflight와 모든 mutation을 봉인하는 durable `rotate-pair` recovery가 partial/stale binding 없이 새 pair를 만든다.
- [/] M05 sibling contract — PinVi isolated admission/activation과 Map attestation이 Map SHA·PinVi SHA·v5 pinset·Manager SHA·v6 execution identity를 exact 대조하도록 함께 이행한다.
- [/] M05 terminal forensic — every terminal one-shot의 raw E2E output·HTTP·container·환경·private receipt를 완주 전까지 gitignored local analysis에 상세 기록하고, tracked 문서·commit·push에는 넣지 않는다.
- [ ] M05 activation — 일반 host-loopback readiness 정책과 ledger 전 rendered Compose publish preflight를 적용한 새 v6 execution candidate에서 `ktdctl` atomic binding, 단발 rebuild, public execution binding, isolated M04/M05 live E2E, activation attestation을 각각 정확히 한 번 통과한다. **2026-09-03 실측**: pinset `e6b52db4`(Map `8078b110` + PinVi `357da189`), Manager `5befecbb`에서 rotate-pair → 단발 rebuild → rebind-execution → isolated M04/M05 live E2E가 `status: passed`로 닫혔다(`m04_attestation_sha256=d5f0c4d0…`, `m05_attestation_sha256=69fb285e…`, `runtime_provenance_sha256=bac562a0…`, `cleanup_failed=false`). 남은 판정은 소유자 몫이다.
- [/] Map/PinVi cross-repo live consumer acceptance — WebSocket/mutating loop·consumer reconciliation과 Manager manifest/journal을 실제 pair에서 교차 대조한다.
- [/] standalone backup 운영 보강 — off-box 사본 자동화와 보존 정책을 완료한다.
- [/] ktdctl UI migration — public generation 관측과 남은 M5~M7 UI 이관을, root CLI authority를 유지한 채 완료한다.
- [/] journal/attestation drift — Manager generation receipt와 Map attestation의 execution binding field를 함께 정렬한다.
- [ ] non-root backend — root ownership을 유지하면서 service-group 접근 경계와 root/서비스 계정 mutation 검증을 완료한다.
- [ ] atomic-write 프리미티브 잔여 통합 (GM-10 후속) — mkstemp 9곳 중 `standalone_backup.py` 1곳만 정본으로 이관됐고 나머지 8곳은 각각 정본 시그니처와 맞지 않는 이유가 있다(TOCTOU 재검사·strict 디렉터리 fsync 계약·hardlink 발행·`recovery_succeeded` 신호원). `runtime_pin_request.py`는 대상이 죽은 코드라 후속은 이관이 아니라 `replace_existing` 플래그 제거다. 조사 기록: `docs/journal.md` 2026-09-03 「tasks.md에서 이관한 조사 기록」.
- [ ] LoginScreen.tsx를 `humanizeError`/`CODE_MESSAGES`로 옮긴다 — 다만 `require_frontend_origin`(403 `INVALID_ORIGIN`)이 bare 문자열이고 `CODE_MESSAGES`에도 없어, 먼저 그것을 봉투화하고 코드 매핑을 추가하지 않으면 원문 토큰이 화면에 노출되는 새 회귀가 생긴다. 조사 기록: `docs/journal.md` 2026-09-03 「tasks.md에서 이관한 조사 기록」.
- [x] GM-17 선행조건 — targets 문서의 자리를 trusted 설치본에 고정하고 무결성을 검증한다 (2026-09-17 완료). `TARGETS_FILE`/`PROJECT_ROOT` env redirect를 설치본에서 거절하고, 읽기를 `O_NOFOLLOW` + `fstat` 기반 검증 descriptor로 바꿔 root 소유·nlink 1·group/other 비쓰기를 강제한다(개발 checkout은 종전 그대로). 감사 노트 (b)가 "그대로 옮기면 보안 회귀"라고 지목한 그 자리다 — **이것이 초록이어야 아래 allowlist 이관이 가능하다.** 변이 셋으로 결박 확인.
- [x] GM-17 본작업 A — bind allowlist 외부화 (2026-09-17 완료, 적대 리뷰 2인 반영). `_CANDIDATE_ALLOWED_OPERATOR_BINDS`(125줄 dict 리터럴)를 `config/docker-targets.yml`의 `compose_binds:` 절로 옮기고 코드에는 검증 규칙만 남겼다. **병목 해소의 범위를 정확히 적는다**(리뷰 H-3 정정): installer가 `git archive`로 트리를 통째 교체하고 넘어오는 것은 `.env` 하나뿐이라, 설치본 config를 직접 고쳐도 다음 재설치에서 되돌아간다. 즉 **backend 코드 수정은 없어졌지만 재설치는 그대로다** — "config 작업이 됐다"는 절반만 참이었다. **값은 한 글자도 바꾸지 않았다** — 옮기기 전후 해석된 매핑 29건이 정확히 같다(sha256 `e7ec261c30db1d04`). 변이(항목 하나 제거)로 3건이 빨개지고 그중 하나가 실제 배포 검증 경로라, 설정이 진짜 소비된다는 것까지 확인했다.
- [ ] GM-17 후속 — 개인 경로 기본값 제거(`${VAR:-/home/digitie/...}` → `${VAR:?}`). **A에서 의도적으로 제외했다.** 감사 노트 실측: 로컬 dev와 n150 prod 둘 다 지금 그 하드코딩된 기본값에만 의존해 돌고 있어서, `.env`를 먼저 갱신하지 않고 필수화하면 다음 `compose up`/재시작이 즉시 깨진다 — "안전한 정리"가 아니라 운영 중단 변경이다. `.env` 갱신과 **조율**해서만 한다.
- [x] 백업 wrapper의 git 실행 비트 (2026-09-17). `scripts/run-standalone-backup.sh`와 `scripts/run-offbox-sync.sh`가 git에 **`100644`**로 들어 있었다. crontab은 그 경로를 **직접 실행**하므로 새 체크아웃마다 `Permission denied`로 죽는다 — 실제로 `geo_dagster`·`concierge`·`pinvi` 백업이 **2026-09-12부터 5일간** 전부 실패했다(마지막 성공 09-11 03:15~03:55Z). Map 원장이 원인을 "배포가 벗겼다"로 적었는데 **틀렸다. 처음부터 없었다.** installer가 `0755`로 세우는 스크립트는 넷(`install-ktdm-trusted-release`·`run-pinned-rebuild-once`·`run-m05-isolated-e2e-once`·`rotate-pinned-pair`)뿐이고 백업 wrapper 둘은 그 목록에 없다(`/opt`의 사본도 `-rw-r--r--`). 그래서 09-16 호스트에서 한 `chmod +x`는 국소 수정이었고 **다음 체크아웃이 되돌렸을 것이다.** git 모드를 `100755`로 고치고, `backend/tests/test_cron_executed_scripts_are_executable.py`가 "헤더에 crontab 직접 실행 줄을 적은 스크립트는 git 모드가 `100755`"를 강제한다 — 목록을 손으로 들지 않고 **스크립트 자신의 주장**에 결박하되, 탐지기가 공허해지지 않게 그 둘을 하한으로 박았다. 복구 실측: 09-17 창에서 세 role 전부 artifact 생성(pinvi `pinvi-1789617309.dump`, GC 1건).
- [ ] 백업 후속 — **crontab이 개발 체크아웃을 실행한다**. n150의 `digitie` crontab 세 줄이 `/home/digitie/kor-travel-docker-manager/scripts/run-standalone-backup.sh`를 부른다 — trusted install(`/opt`)이 아니라 **개발 체크아웃**이다. 그래서 그 체크아웃에서 브랜치를 옮기거나 파일을 건드리는 모든 행위가 prod 백업의 실행 가능성을 좌우한다(2026-09-12 사고의 증폭 요인이다 — git 모드가 원인이었지만, 개발 체크아웃을 물고 있지 않았다면 배포 대상이 아닌 자리에서 조용히 깨지지는 않았다). 위 installer 수정으로 `/opt` 사본도 `0755`가 되므로 이제 옮길 수 있다. 옮기려면 재설치 1회 + crontab 3줄 수정이고, 둘 다 prod 쓰기라 **오너 승인 후** 한다.
- [ ] GM-17 후속 — **target-side** bind 정책. A의 적대 리뷰(2026-09-17)가 정정했다: 내가 "`rustfs-init` 때문에 값 정책을 못 넣는다"고 적은 것은 틀렸다 — 29건 전수 확인 결과 manager 경로는 `container_path` 쪽에만 있고 `source` 쪽은 **0건**이다. 그래서 **source-side 정책은 A에서 함께 넣었다**(`_assert_operator_bind_source_is_permitted`). 남은 것은 target-side뿐이고, 그것은 `rustfs-init`의 예외를 먼저 분류해야 규칙을 세울 수 있다.
- [ ] GM-17 후속 — allowlist 변경의 **즉시 반영**. 상주 root uvicorn backend는 `load_targets_config`를 프로세스당 한 번만 읽고 installer가 재기동하지 않으므로(`deploy/systemd/ktdm-backend.service`), 운영자가 위험한 bind를 지워도 재기동 전까지 옛 목록으로 candidate가 통과한다. **취소가 즉시 반영되지 않는다.** mutation 진입 시 fd 기준 `(st_dev, st_ino, st_mtime_ns, st_size)` 재확인 후 재로드가 후보다.
- [ ] GM-17 후속 — `ktdctl targets validate`에 **semantic 검사** 이관. A가 추가한 교차 검사(서비스가 compose에 존재, 죽은 grant 없음)는 pytest 안에 있어 저장소 사본에서만 돈다. 설치본 문서에 실제로 적용되는 것은 형태 규칙뿐이라, 운영자 편집이 실제 흐름이 되면 그 격차가 사고 경로가 된다.
- [x] GM-17 B · S0 — 서비스 부재 형상 골든 테이블 (2026-09-17 완료, 동작 변경 0). 완화의 폭발 반경이 CI에 안 보이는 것이 B의 가장 큰 위험이라, 코드를 고치기 전에 "오늘 어떤 부재가 어떤 메시지로 거부되는가"를 표로 박았다. 실측이 드러낸 것: **여섯 형상 중 부재를 부재라고 말하는 것은 하나뿐**이고, 서비스를 `null`로 만들면 무관한 Map 메시지가 나온다. 이후 단계의 PR은 이 표의 diff를 본문에 싣고 **"거부 → 통과"로 바뀐 칸을 전부 의도한 완화로 열거**해야 한다.
- [x] GM-17 B · S1 — 무조건 인덱싱 제거 + 검사 순서 교정 (2026-09-17 완료, 판정 변경 0). required-set 검사를 여섯 validator 앞으로 옮기고, 서비스 값 모양 검사(`null`은 부재가 아니라 invalid — S4의 안전 조건이다)를 더하고, 15개 이름 루프의 무조건 인덱싱을 `.get()` + 계약 오류로 바꿨다. **required 집합은 한 글자도 바꾸지 않았다.** 판정 변경 0은 적대 리뷰가 독립으로 재확인했다 — 형상 36종 × 진입점 2개 = **72칸에서 verdict flip 0, 예외 타입 변경 0**. 순서 교정의 파급이 기존 검사 하나에서 드러나 fragment를 완전하게 만들어 의도를 보존했다. **정정(적대 리뷰 2026-09-17)**: 첫 기록이 "`absent_*` → missing required protected services"라고 단정했는데 **15개 중 하나에서 거짓**이다. `pinvi-db-init`은 frozenset(14) 밖이라 `_validate_pinvi_db_init_identity`가 먼저 걸러 `PinVi database init identity is invalid`를 낸다. 실질 15의 정체가 바로 그 비대칭이고, 이제 골든 테이블이 그 예외를 명시적으로 박는다.
- [x] GM-17 B · S0/S1 적대 리뷰 반영 (2026-09-17). 리뷰어 둘이 **각각** 같은 급소를 찾았다 — **골든 테이블이 자기 목적을 수행하지 못했다**. 첫 판은 `PASS/not-PASS`만 봤는데, required 집합을 `frozenset()`으로 비워도, 이름 하나를 빼도 전체 스위트가 초록이었다. 완화는 "거부 → 통과"가 아니라 **"거부 이유 A → 거부 이유 B"**로 움직이기 때문이다(S1이 넣은 `.get()` 가드가 전부 받아낸다). 고친 것: (1) 15개 서비스 **단독 부재**를 전수로 돌고 **이유를 박는다**, (2) **resolved 열 추가** — 프로덕션 diff의 절반이 무증거였다 (resolved 쪽 수정 셋을 전부 되돌려도 초록이었다), (3) required 집합과 15개 루프 이름을 **리터럴로** 고정(파생하면 S4가 비웠을 때 표도 함께 비어 공허하게 초록이 된다), (4) 죽은 중복 `isinstance` 블록 2곳 제거 — 하필 죽은 쪽이 raw/resolved를 **구분하는** 문구를 갖고 있었다, (5) 15개 루프에서 **부재와 invalid를 분리**(`.get()`이 둘을 `None` 하나로 뭉갠다 — S4에서 그대로 두면 S1이 없앤 혼동이 한 층 아래에서 되살아난다), (6) 후보가 정한 **임의 서비스 키가 계약 오류 문구로 에코**되던 것을 sha8로 가림 (그 문구는 CLI stderr와 HTTP body로 나가고, 이 루프는 보호값 전역 스캔보다 앞이다), (7) `_verdict`의 200자 절단 제거 — 그 절단 때문에 변이 하나가 **우연히** 죽어 있었다. 변이 검증: 리뷰가 살아남았다고 보고한 여섯(required에서 이름 하나 제거 ×2, 무조건 인덱싱, shape 루프 한정, resolved만 순서 되돌림, 완화+인덱싱)이 **전부 빨개진다**.
- [x] GM-17 B 후속 — **죽은 계약 테스트 3건** (2026-09-17 완료). `backend/tests/test_docker_service_config.py`의 `:1905`·`:2257`·`:2324`는 `pytest.raises(ComposeCandidateContractError)`에 `match=`가 없고, compose 문서에 `rustfs` 하나뿐이라 required 14개가 전부 빠져 있다. 실제로는 함수 맨 앞의 `_validate_map_production_secret_values`가 `KOR_TRAVEL_MAP_ADMIN_PROXY_SECRET must contain at least 32 characters`로 먼저 던지고, **payload를 아예 비워도 통과한다**(적대 리뷰 2026-09-17 실측). 겨냥한 보호 이름 스캔에는 도달조차 하지 않는다. **S4가 착지할 자리가 정확히 여기다** — 서비스 없는 compose로 mutation 경계를 덮는 유일한 테스트인데 아무것도 태우지 않는다. `match=` 추가 + `_prepare_candidate_transaction`(`:1769`)의 env 분기가 rustfs-only 문서에도 걸리게 고칠 것.
- [ ] GM-17 B 후속 — **개선된 진단이 운영자 화면에 도달하지 않는다**. `main.py:336`이 HTTP 409로 `{code, message}`를 내는데 `ComposeCandidateContractError.code`는 모든 인스턴스가 공유하는 단일 상수(`COMPOSE_CANDIDATE_PROTECTED_REFERENCE`)이고, `frontend/src/lib/errors.ts:172-180`의 `humanizeError`는 **코드 매핑을 서버 메시지보다 우선**한다. 그래서 실제 원인이 `missing required protected services: …`여도 헤드라인은 여전히 "변경하려는 값이 보호된 항목이라 적용하지 않았습니다"이고 새 문구는 접힌 raw 안에만 있다. 부재 전용 code + `CODE_MESSAGES` 항목이 필요하다. **API 오류 계약 변경이므로 "판정 변경 0"인 S0/S1에 넣지 않고 분리한다.**
- [ ] GM-17 B 후속 — **Map에는 `command` 검사가 아예 없다**(적대 리뷰 2026-09-17 실측). `pinvi-postgres`의 `command`는 `listen_addresses=127.0.0.1`을 강제하는데 `kor-travel-map-postgres`의 `command`는 **어느 validator도 보지 않는다** — 실측: Map은 `listen_addresses=*`로 바꿔도 PASS, PinVi는 `PinVi PostgreSQL identity is invalid`로 거부. S4가 Map family를 완화하기 전에 알아야 할 비대칭이고, GM-17 B 서사가 가정한 것보다 Map 쪽 그물이 성글다.
- [ ] GM-17 B 후속 — **required 목록이 두 곳이다**. `_CANDIDATE_REQUIRED_PROTECTED_SERVICES`(14) 외에 보호 이름 스캔 안에 **두 번째 하드코딩 15개 루프**가 따로 있다(raw·resolved 각 1곳). S4는 **둘 다** 풀어야 하고, 그때 "오늘은 도달 불가"라는 논거들도 함께 무너진다 — 최상위 `secrets` 선언 검사와 (B)의 미검증 분기들이 그때 처음 도달 가능해진다.
- [ ] GM-17 B 후속 — **잠복 fragment 2건**. `test_f1d_compose_contract.py:1917`·`:1957`은 fragment에 required 서비스 `pinvi-db-runtime-role`이 빠져 있다. 지금은 `_validate_map_database_dsn_identities`가 더 앞이라 살아남고 `match=`가 있어 조용히 죽지도 않지만, S2/S3가 순서를 또 건드리면 첫 희생자다. S1에서 한 것과 같은 처방(fragment를 완전하게)을 적용할 것.
- [x] GM-17 B · S2 — Map 계열 validator 삼분할 (2026-09-17 완료, **진입점 기준** 동작 변경 0). `_validate_map_postgres_password_secret`을 셋으로 쪼갰다: (A) `_validate_map_postgres_password_owner_wiring` — 소유자 배선, 소유자 부재 시 건너뛴다; (B) `_assert_map_postgres_password_sole_consumer` — 유일 소비자 스캔; (D) `_validate_map_postgres_password_declaration` — 최상위 `secrets` 선언. **(B)와 (D)는 family validator 블록 밖, 진입점에 직접 있다.** 인가 집합은 `_authorized_map_postgres_password_reference`가 독립 파생하며 **모양(source/target)까지 검증**하고 소유자가 없으면 공집합이다. **적대 리뷰 둘이 각각 첫 판을 뚫었다.** 처음에는 (B)·(D)를 Map validator 안에 두고 "합성 진입점에 결박했다"고 적었는데, S4가 자르는 층은 그 위였다 — 진입점의 호출부를 소유자 존재로 감싸는 순진한 S4가 **전체 스위트 1,700건을 그대로 통과**했고, 그 상태에서 `pinvi-api`가 Map superuser password를 마운트하는 후보가 mutation 경계를 통과했다. 이중화로 막으려 한 두 번째 시도도 틀렸다 — 둘 중 하나를 지우는 변이를 **아무 검사도** 잡지 못했다(검사 불가능한 방어). 최종적으로 자리를 **하나**로 만들고 wrapper를 제거했다. 리뷰가 실증한 그 시나리오를 재현해 확인: 게이팅 + required 축소 + 15개 루프 우회를 모두 적용해도 이제 `unauthorized consumer`로 **거부**된다. 리뷰가 찾은 선재 공백 둘도 메웠다 — 소유자의 exact-target 불변식(지워도 1,700건이 전부 통과했다)과 짧은 문법(`secrets: ["<이름>"]`) 참조 축. 정정 둘: `authorized is None` 논리합은 **오늘 판정을 바꾸지 않는다**(1,782문서 × 2형상에서 변이와 동치) — S4를 위해 남기되 그것이 지금 일한다고 주장하지 않는다. 그리고 함수 기준으로는 8칸이 달라진다(전부 required-set이 막는 소유자-부재 영역) — **진입점 기준** 0이다.
- [x] GM-17 B · S3-a — PinVi PostgreSQL 신원을 db-init 게이트에서 분리 (2026-09-17 완료, 동작 변경 0). 종전 `_validate_pinvi_db_init_identity`는 맨 앞에서 `pinvi-db-init` 부재를 즉시 거부한 뒤, **같은 함수 안에서** `pinvi-postgres`의 image·environment·command를 검사했다. 그 command 배열이 `listen_addresses=127.0.0.1`을 강제하는 **저장소에서 유일한 자리**다. 따라서 S4가 그 함수를 db-init 존재로 게이팅하면 PostgreSQL의 loopback 결박이 통째로 사라진다 — 네트워크 노출 통제라 S2의 secret 소비자 스캔보다 결과가 나쁘다. **순서를 보존하려고 둘이 아니라 셋으로 나눴다**: (1) `_validate_pinvi_db_init_presence` (db-init 게이트 대상) → (2) `_validate_pinvi_postgres_identity`(**pinvi-postgres** 게이트 대상, loopback 포함) → (3) `_validate_pinvi_db_init_command`(db-init 게이트 대상). 둘로 나누면 (2)와 (3)의 선후가 뒤집혀 두 결함이 동시에 있는 문서의 문구가 바뀐다. 분할 중 (2) 안에 **db-init의 image를 보는 한 줄**이 있는 것을 발견했다(자리가 두 postgres 검사 사이라 옮기면 순서가 바뀐다) — 자리를 두고 존재-조건부로만 만들었다. **변이 검증에서 내 첫 시뮬레이션이 뚫렸다**: db-init 전용 validator만 no-op으로 만들고 **문서에는 db-init을 남겨 둬서**, 게이팅 변이가 검사 64건을 전부 통과했다. S2에서 배운 것과 같은 실패다 — S4가 실제로 만드는 형상(서비스가 사라진 상태)에 결박해야 한다. 고친 뒤 변이 넷이 각각 2·4·2·2건을 빨갛게 만든다. `listen_addresses` 강제가 한 곳뿐이라는 전제 자체도 검사로 박았다.
- [x] GM-17 B · S3-b — PinVi password validator 삼분할 (2026-09-17 완료, **판정 변경 0 / 진단 문구는 정정 후 0**). 선언 검사와 유일 소비자 스캔은 family scope가 걸리지 않는 전역 불변식으로, 소유자 배선만 조건부다. 인가 파생은 **모양까지 검증**한다 — PinVi 소유자 참조는 짧은 문법과 두 가지 target을 허용하므로 무검증 파생은 Map보다 위험하다. 인가 집합이 셋인 것이 PinVi의 특징이다(소유자는 파생, `pinvi-db-init`·`pinvi-db-runtime-role`은 **리터럴**이라 소유자와 무관하게 유효하다). **적대 리뷰 둘이 각각 첫 판을 뚫었다.** (1) 전역 둘의 **자리**를 옮긴 탓에 진입점 메시지가 바뀌었다 — 리뷰 A는 1,434형상에서 **215칸**, 리뷰 B는 66형상에서 8칸. 내 추론의 구멍: 소비자 스캔을 family 블록 뒤에 두면서 (A) 대 (B)의 선후만 따졌는데, 종전에 그 스캔은 **같은 family의 validator 전부보다 앞**이었다. 실제 권한상승 시도가 image provenance 문구 뒤로 숨는 형상이 생겼다 — **S1이 고친 바로 그 병이다.** 리뷰가 정정한 개념: **자리(position)와 게이팅(gating)은 다른 축**이고, "family 블록 밖"은 위치가 아니라 **scope 조건이 걸리지 않음**을 뜻한다. 전역 여섯을 원래 자리로 되돌려 고쳤고, **S2 이전(`df40e57`) 대비 78개 이중결함 형상에서 차이 0**으로 확인했다. 그 과정에서 **S2도 같은 회귀를 냈다는 것이 드러났다** — 현 main(`9bfabea`) 대비 8칸이 다른데 전부 Map 쪽이고, 이 PR이 그것까지 함께 고친다. (2) "Map(S2)과 같은 배치다"는 **코드 배치에 대해서만** 참이고 **검증에 대해서는 거짓**이었다 — S2가 Map에 넣은 결박 검사 넷이 PinVi에 복제되지 않아, 전역 둘을 게이팅·인라인·간접호출하는 변이 넷과 소유자 배선의 핵심 검사 셋이 **전부 생존**했다. 진입점 S4 시뮬레이션 검사를 넣어 열 변이가 모두 빨개진다. (3) `entrypoint` 가드가 양쪽 다 무검사였다 — `entrypoint`를 주면 `command`는 인자가 되므로 **command를 한 글자도 안 바꾸고** loopback을 무력화할 수 있는데, 그 유일한 방어를 지우고도 1,721건이 초록이었다. 검사를 넣었다. (4) `test_the_loopback_binding_has_exactly_one_home`이 **파일 목록**을 비교해 같은 파일의 두 번째 자리를 못 봤다(docstring이 거짓이었다). AST로 **코드 리터럴 출현 횟수**를 세도록 고쳤다 — 문자열을 그냥 세면 설명을 더 쓸 때마다 빨개져서 결박이 아니라 잡음이 된다. (5) S4 시뮬레이션이 frozenset만 패치하고 **하드코딩 15개 루프**를 안 풀어 목표 지점에 닿기 전에 막히고 있었다. 둘 다 패치하도록 고쳤다.
- [ ] **Map postgres가 image·command·entrypoint·INITDB_ARGS 전부 무검사다** (적대 리뷰 2026-09-17 20필드 전수 실측). 종전 기록("Map에는 `command` 검사가 없다")은 빙산의 일각이었다. 가장 위험한 것은 **`POSTGRES_INITDB_ARGS=--auth-host=trust`가 Map에서 PASS**라는 것이다 — fresh PGDATA에서 **superuser 인증을 통째로 끄는** 값인데, `_CONTRACT_LOCKED_ENV_NAMES_BY_SERVICE`가 `POSTGRES_INITDB_ARGS`를 `pinvi-postgres` **에만** 걸어 두었다. loopback 결박도 Map에는 없으므로 "Map postgres를 모든 인터페이스에 열고 trust 인증으로 초기화"하는 후보가 통과한다. **GM-17 B S4보다 우선순위가 높다** — S4가 Map family를 완화하는 순간 그물이 아예 없다. (`privileged`·`cap_add`·`user`·`pid`가 양쪽 다 무검사인 것은 별개 선재 공백.)
- [ ] GM-17 B · S3-c — `_validate_pinvi_database_url_identities`의 **환경 블록 분리** (적대 리뷰 M-4). 그 함수 앞부분은 서비스와 무관한 전역 불변식이다 — `PINVI_DB_PORT == 12800` 강제(저장소 유일), role 이름 5개의 상호 상이성, root/app/migrator **password 3자 상호 비동일**(`hmac.compare_digest` ×3, 저장소 유일). 그것이 S4가 family 단위로 게이팅할 함수 호출 하나 뒤에 통째로 들어 있다 — **S3-a가 loopback에 대해 고친 것과 정확히 같은 모양**이다. Map은 이미 옳다(`_validate_map_database_dsn_identities(environment)`는 `environment`만 받고 services 검사보다 앞이다). **S4 착수 전에 할 것.**
- [ ] GM-17 B 후속 — `expected_*` 사본 둘이 조용히 갈라질 수 있다 (리뷰 M-3). `_validate_pinvi_db_init_presence`가 넷을 파생하고 `_validate_pinvi_postgres_identity`가 셋을 **독립으로 재계산**한다. 종전에는 한 지역 변수라 구조적으로 일치했다. 오늘 그 우연을 지키는 것은 `PINVI_DB_PORT == 12800` 강제 한 줄뿐인데 그것이 S3-c의 대상이다. 처방: `PINVI_DB_PORT`·`PINVI_POSTGRES_BOOTSTRAP_DB`를 비기본값으로 주입한 환경으로 진입점을 태우는 파라미터 축 검사(테스트 fixture가 그 둘을 아예 안 줘서 "env를 읽는가"가 검사되지 않는다). 삭제된 docstring "reset 대상과 같은 loopback DB를 가리키는지"도 복원할 것.
- [ ] GM-17 B 후속 — db-init **resolved** environment 검사에 음성 커버리지 0 (리뷰 M-5). raw 분기는 frozen bootstrap 검사가 잡는데 resolved 분기는 no-op으로 만들어도 전부 초록이다. resolved가 실제 배포에 적용되는 쪽이고, S1 이후 "resolved 열이 없어서 프로덕션 diff의 절반이 무증거였다"와 같은 종류다.
- [ ] GM-17 B 후속 — `pinvi-db-init`의 `image` 키가 **없으면** provenance 검사가 조용히 통과한다 (리뷰 L-5, `.get("image")`가 `None`을 돌려주는 경로). 오늘은 (1)이 `environment`까지 요구해 위험이 낮지만 S4가 (1)을 게이팅하면 열린다. 조건을 "존재하면 image가 정확히 `_PINVI_POSTGRES_IMAGE`"로 강하게 쓸 것.
- [ ] GM-17 B 후속 — `_validate_pinvi_db_runtime_role`의 `depends_on`이 `pinvi-db-init: service_completed_successfully`를 리터럴로 요구한다. S4가 db-init만 scope에서 빼고 role을 남기면 즉시 죽는다 — 선행 조사 (2)의 런타임 결합 목록에 추가.
- [ ] GM-17 B · S4 — family scope 도입 (**유일한 실제 완화**). 축은 mode도 호출부도 아니라 `in_scope(F) = declared(F) OR witnessed(F)`다. declared는 `config/docker-targets.yml`이 주고(GM-17 A가 이미 그 문서를 신뢰시켜 뒀다), witnessed는 문서의 흔적 — **하한이지 상한이 아니다**(요구를 더할 뿐 빼지 못한다). 이 형태가 자가무장해제를 막는다: targets가 `map`을 선언하는 한 compose에서 한 줄 지워도 계속 거부된다. 되돌리기는 "모든 family 항상 in_scope" 한 줄 kill switch.
- [x] GM-17 B · 선행 조사 (1) `running_from_trusted_install_root()` (2026-09-17 실측, **참**). n150의 두 실행 경로 모두 `/opt/kor-travel-docker-manager/backend/.venv`에서 돈다 — 상주 backend(`ktdm-backend.service` `ExecStart`, active·enabled, 12901 리스닝)와 pinned-rebuild 런처(`run-pinned-rebuild-once:319`가 `/opt/.../\.venv/bin/ktdctl`). 술어를 그 venv에서 직접 태워 확인했다: `__file__`이 trusted root 아래(분기 1 참), `sys.prefix == TRUSTED_INSTALL_ROOT/backend/.venv`(분기 2 참) → `True`. 호스트의 다른 `ktdctl`은 `/home/digitie/ktdm-gate/.venv` 하나뿐인데 **2026-08-30 #291 시점 체크아웃이라 `trusted_install` 모듈 자체가 없다**(`ModuleNotFoundError`) — 없는 메커니즘을 우회할 수는 없고, prod 검증 경로에 결선돼 있지도 않다. **결론: `declared` 축(= `config/docker-targets.yml`, GM-17 A가 신뢰시킨 문서)의 권위가 S4가 의존하는 모든 경로에서 유지된다.**
- [x] GM-17 B · 선행 조사 (2) 런타임 전제 전수 (2026-09-17). **"15개를 전제하는 자리"는 하나가 아니다 — 네 모듈에 걸친 여섯 집합이고, S4가 완화하는 것은 그중 하나뿐이다.**

  | 집합 | n | 모듈 | S4 대상 |
  |---|---|---|---|
  | `_CANDIDATE_REQUIRED_PROTECTED_SERVICES` | 14 | `c6c_deployment` (검증) | **이것뿐** |
  | 15개 소비자 루프(= required 14 + `pinvi-db-init`) | 15 | `c6c_deployment` (검증) | 따라온다 |
  | `_MAP_RUNTIME_SERVICES` | 4 | `c6c_deployment` (검증) | 따라온다 |
  | `RUNTIME_SERVICES` (핀셋) | 7 | `pinned_runtime_generation` | **아니다 — 런타임** |
  | `COMPOSE_BUILT_RUNTIME_SERVICES` | 4 | `pinned_runtime_rebuild` | **아니다** |
  | `_PINNED_RUNTIME_ONESHOT_WRITERS` | 8 | `compose_service` | **아니다 — 여기가 터진다** |

  검증 **밖**의 경성 전제 셋을 찾았고, 전부 `docker compose`에 서비스 이름을 그대로 넘긴다:

  1. `compose_service.py:5150 _retire_pinned_runtime_oneshot_writers` — 8개 이름을 `compose rm -f -s` / `compose ps --all`에 그대로 넘긴다. **실측**(n150, 더미 compose): 정의되지 않은 서비스를 주면 `no such service: <이름>`이다. 그 8개는 전부 required(14) ∪ {`pinvi-db-init`} 안에 있으므로 **S4가 한 family를 빼는 순간 최대 8개가 사라지고 이 호출이 죽는다.**
  2. `compose_service.py:8056 _require_services_ready` — 같은 방식으로 `ps`에 넘기고, 실패하면 `cannot inspect mandatory service readiness`. `RUNTIME_SERVICES`(7)와 `("kor-travel-map-postgres", "pinvi-postgres")`에 쓰인다.
  3. `compose_service.py:6894-6912` — 위 두 개짜리 호출의 결과를 **위치로** 인덱싱한다(`postgres_records[0]`은 Map, `[1]`은 PinVi로 가정해 각각 `validate_map_postgres_runtime_secret_isolation`·`validate_pinvi_postgres_runtime_secret_isolation`에 넘긴다). 한쪽 family가 빠지면 이 대응이 **조용히 어긋난다**.

  **비대칭 하나 더**: `RUNTIME_SERVICES`에는 `pinvi-web`·`pinvi-dagster`가 있는데 required(14)에는 **없다**. 즉 핀셋은 검증이 요구하지 않는 서비스의 존재를 이미 전제한다.

  **결론 — S4의 범위가 커진다.** family scope는 validator만 통과시키면 끝나는 것이 아니라 위 런타임 집합에도 같은 scope를 먹여야 한다. 그러지 않으면 원장이 경고한 그대로 **"이른 거부"가 "핀셋 소모 후 런타임 실패"로 바뀐다** — 그것도 `rm`/`ps`가 뱉는 `no such service`라는, 원인을 말하지 않는 문구로. S1이 진단을 고친 의미가 그 지점에서 사라진다.
- [ ] GM-17 본작업 B — required-set 완화. dev ensure에서 15개 서비스 존재 강제를 존재-조건부로 바꾼다(frozenset 14 + `_PINVI_DB_INIT_SERVICE` 별도 강제 = 실질 15). 감사 노트 (d)대로 production ensure는 이미 원천 거부(`compose_service.py:4663-4676`)라 "production 모드로 한정"은 ensure에 한해 공허하고, 실제 적용 대상은 pinned-rebuild와 production save/mutation 경로다. **다수 cross-service validator를 존재-조건부로 바꾸는 광범위 감사가 필요하고 effort L, 그 이하로 축소 불가**(노트 원문). 문서 전역 보호 이름/값 스캔은 무조건 유지한다. **착수 전 오너와 범위를 재확인할 것.**
