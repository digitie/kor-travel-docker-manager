# 줄바꿈은 저장소 전체에서 LF다. Windows 호스트에서 파일을 고칠 때 CRLF로 바뀌면
# 실제 변경 한 줄이 파일 전체 재작성으로 커밋된다 (2026-08-31 실제 발생:
# 579줄 변경이 30,240줄로 보였다). `*.sh`만 고정돼 있어 `.py`/`.md`가 새고 있었다.
* text=auto eol=lf

*.sh text eol=lf
