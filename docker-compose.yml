services:
  kraddr-geo-postgres:
    image: postgis/postgis:16-3.5
    container_name: kraddr-geo-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${KRADDR_GEO_POSTGRES_USER:-addr}
      POSTGRES_PASSWORD: ${KRADDR_GEO_POSTGRES_PASSWORD:-addr}
      POSTGRES_DB: ${KRADDR_GEO_POSTGRES_DB:-kraddr_geo}
      TRIPMATE_POSTGRES_USER: ${TRIPMATE_POSTGRES_USER:-tripmate}
      TRIPMATE_POSTGRES_PASSWORD: ${TRIPMATE_POSTGRES_PASSWORD:-tripmate_dev_password}
      TRIPMATE_POSTGRES_DB: ${TRIPMATE_POSTGRES_DB:-tripmate}
      KOR_TRAVEL_CONCIERGE_POSTGRES_DB: ${KOR_TRAVEL_CONCIERGE_POSTGRES_DB:-kor_travel_concierge}
      KRTOUR_MAP_POSTGRES_USER: ${KRTOUR_MAP_POSTGRES_USER:-krtour_map}
      KRTOUR_MAP_POSTGRES_PASSWORD: ${KRTOUR_MAP_POSTGRES_PASSWORD:-krtour_map_dev_password}
      KRTOUR_MAP_POSTGRES_DB: ${KRTOUR_MAP_POSTGRES_DB:-krtour_map}
      KRADDR_GEO_STRICT_SOURCE_CHECK: ${KRADDR_GEO_STRICT_SOURCE_CHECK:-1}
      KRADDR_GEO_SOURCE_DIR: ${KRADDR_GEO_SOURCE_DIR:-/data/juso}
    ports:
    - ${KRADDR_GEO_DB_PORT:-5432}:5432
    volumes:
    - ${KRADDR_GEO_PGDATA:-/home/digitie/kraddr-geo-data/pgdata-final-20260529}:/var/lib/postgresql/data
    - ${KOR_TRAVEL_GEO_JUSO_DATA:-/mnt/f/dev/kor-travel-geo/data/juso}:/data/juso:ro
    - ./scripts/ensure-kraddr-geo-db.sh:/docker-entrypoint-initdb.d/010-ensure-kraddr-geo-db.sh:ro
    - ./scripts/ensure-kraddr-geo-db.sh:/opt/tripmate-manager/ensure-kraddr-geo-db.sh:ro
    - ./scripts/verify-kraddr-geo-source.sh:/opt/tripmate-manager/verify-kraddr-geo-source.sh:ro
    shm_size: 512mb
    command:
    - postgres
    - -c
    - shared_preload_libraries=pg_stat_statements
    - -c
    - pg_stat_statements.track=all
    - -c
    - pg_stat_statements.max=10000
    - -c
    - shared_buffers=512MB
    - -c
    - work_mem=64MB
    - -c
    - maintenance_work_mem=256MB
    - -c
    - max_wal_size=2GB
    - -c
    - checkpoint_completion_target=0.9
    - -c
    - effective_cache_size=1536MB
    - -c
    - random_page_cost=1.1
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -U ${KRADDR_GEO_POSTGRES_USER:-addr} -d ${KRADDR_GEO_POSTGRES_DB:-kraddr_geo}
      interval: 10s
      timeout: 5s
      retries: 5
  rustfs:
    image: rustfs/rustfs:latest
    container_name: tripmate-rustfs
    restart: unless-stopped
    environment:
      RUSTFS_ACCESS_KEY: ${RUSTFS_ACCESS_KEY:-rustfsadmin}
      RUSTFS_SECRET_KEY: ${RUSTFS_SECRET_KEY:-rustfsadmin}
      RUSTFS_VOLUMES: /data
      RUSTFS_ADDRESS: :${RUSTFS_API_CONTAINER_PORT:-9000}
      RUSTFS_CONSOLE_ENABLE: 'true'
      RUSTFS_CONSOLE_ADDRESS: :${RUSTFS_CONSOLE_CONTAINER_PORT:-9001}
    ports:
    - ${RUSTFS_API_PORT:-12101}:${RUSTFS_API_CONTAINER_PORT:-9000}
    - ${RUSTFS_CONSOLE_PORT:-12105}:${RUSTFS_CONSOLE_CONTAINER_PORT:-9001}
    volumes:
    - ${RUSTFS_DATA_DIR:-/home/digitie/kraddr-geo-data/rustfs}:/data
    healthcheck:
      test:
      - CMD-SHELL
      - curl -fsS http://localhost:${RUSTFS_API_CONTAINER_PORT:-9000}/health/live
        || exit 1
      interval: 10s
      timeout: 5s
      retries: 5
  rustfs-init:
    image: minio/mc:latest
    depends_on:
      rustfs:
        condition: service_healthy
    environment:
      RUSTFS_API_CONTAINER_PORT: ${RUSTFS_API_CONTAINER_PORT:-9000}
      RUSTFS_ACCESS_KEY: ${RUSTFS_ACCESS_KEY:-rustfsadmin}
      RUSTFS_SECRET_KEY: ${RUSTFS_SECRET_KEY:-rustfsadmin}
      TRIPMATE_RUSTFS_BUCKET: ${TRIPMATE_RUSTFS_BUCKET:-tripmate-media}
      KRADDR_GEO_RUSTFS_BUCKET: ${KRADDR_GEO_RUSTFS_BUCKET:-kraddr-geo}
      KRTOUR_MAP_RUSTFS_BUCKET: ${KRTOUR_MAP_RUSTFS_BUCKET:-krtour-map}
      KRTOUR_MAP_OFFLINE_UPLOAD_BUCKET: ${KRTOUR_MAP_OFFLINE_UPLOAD_BUCKET:-krtour-uploads}
    volumes:
    - ./scripts/ensure-rustfs-buckets.sh:/opt/tripmate-manager/ensure-rustfs-buckets.sh:ro
    entrypoint:
    - /bin/sh
    - /opt/tripmate-manager/ensure-rustfs-buckets.sh
  kraddr-geo-api:
    image: ${KRADDR_GEO_API_IMAGE:-kraddr-geo-api:latest-main-gdal}
    build:
      context: ${KOR_TRAVEL_GEO_REPO_DIR:-../kor-travel-geo}
      dockerfile: docker/api.Dockerfile
    container_name: ${KRADDR_GEO_API_CONTAINER:-kraddr-geo-api-latest}
    restart: unless-stopped
    depends_on:
      kraddr-geo-postgres:
        condition: service_healthy
      rustfs:
        condition: service_healthy
    environment:
      PORT: ${KRADDR_GEO_API_CONTAINER_PORT:-12201}
      KRADDR_GEO_API_HOST: 0.0.0.0
      KRADDR_GEO_PG_DSN: ${KRADDR_GEO_DOCKER_PG_DSN:-postgresql+psycopg://addr:addr@kraddr-geo-postgres:5432/kraddr_geo}
      KRADDR_GEO_GEOIP_GATE_MODE: ${KRADDR_GEO_GEOIP_GATE_MODE:-off}
      KRADDR_GEO_OPS_TABLE_STATS_CAPTURE_INTERVAL_MINUTES: ${KRADDR_GEO_OPS_TABLE_STATS_CAPTURE_INTERVAL_MINUTES:-0}
      KRADDR_GEO_RUSTFS_ENABLED: ${KRADDR_GEO_RUSTFS_ENABLED:-1}
      KRADDR_GEO_RUSTFS_ENDPOINT_URL: http://rustfs:${RUSTFS_API_CONTAINER_PORT:-9000}
      KRADDR_GEO_RUSTFS_BUCKET: ${KRADDR_GEO_RUSTFS_BUCKET:-kraddr-geo}
      KRADDR_GEO_RUSTFS_PREFIX: ${KRADDR_GEO_RUSTFS_PREFIX:-kor-travel-geo}
      KRADDR_GEO_RUSTFS_ACCESS_KEY: ${RUSTFS_ACCESS_KEY:-rustfsadmin}
      KRADDR_GEO_RUSTFS_SECRET_KEY: ${RUSTFS_SECRET_KEY:-rustfsadmin}
      KRADDR_GEO_RUSTFS_LOCAL_IMPORT_ROOTS: /data
      KRADDR_GEO_VWORLD_API_KEY: ${KRADDR_GEO_VWORLD_API_KEY:-}
    ports:
    - ${KRADDR_GEO_API_PORT:-12201}:${KRADDR_GEO_API_CONTAINER_PORT:-12201}
    volumes:
    - ${KOR_TRAVEL_GEO_APP_DATA_DIR:-../kor-travel-geo/data}:/data:ro
    extra_hosts:
    - host.docker.internal:host-gateway
    healthcheck:
      test:
      - CMD-SHELL
      - >-
        python -c "import urllib.request; urllib.request.urlopen('http://localhost:${KRADDR_GEO_API_CONTAINER_PORT:-12201}/v1/healthz', timeout=5).read()"
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s
  kraddr-geo-ui:
    image: ${KRADDR_GEO_UI_IMAGE:-kraddr-geo-ui:latest-main}
    build:
      context: ${KOR_TRAVEL_GEO_REPO_DIR:-../kor-travel-geo}/kraddr-geo-ui
    container_name: ${KRADDR_GEO_UI_CONTAINER:-kraddr-geo-ui-latest}
    restart: unless-stopped
    depends_on:
      kraddr-geo-api:
        condition: service_healthy
    environment:
      PORT: ${KRADDR_GEO_UI_CONTAINER_PORT:-12205}
      HOSTNAME: 0.0.0.0
      KRADDR_GEO_API_INTERNAL_URL: http://kraddr-geo-api:${KRADDR_GEO_API_CONTAINER_PORT:-12201}
      NEXT_PUBLIC_API_BASE_URL: ${KRADDR_GEO_UI_PUBLIC_API_BASE_URL:-/api/proxy}
      NEXT_PUBLIC_VWORLD_API_KEY: ${NEXT_PUBLIC_VWORLD_API_KEY:-}
      KRADDR_GEO_VWORLD_API_KEY: ${KRADDR_GEO_VWORLD_API_KEY:-}
    ports:
    - ${KRADDR_GEO_UI_PORT:-12205}:${KRADDR_GEO_UI_CONTAINER_PORT:-12205}
    extra_hosts:
    - host.docker.internal:host-gateway
    healthcheck:
      test:
      - CMD-SHELL
      - >-
        node -e "fetch('http://localhost:' + (process.env.PORT || '12205')).then((r) => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s
  prometheus:
    image: ${PROMETHEUS_IMAGE:-prom/prometheus:v2.53.1}
    container_name: ${PROMETHEUS_CONTAINER:-tripmate-prometheus}
    restart: unless-stopped
    user: ${PROMETHEUS_RUN_AS_USER:-0}
    command:
    - --config.file=/etc/prometheus/prometheus.yml
    - --storage.tsdb.path=/prometheus
    - --web.console.libraries=/usr/share/prometheus/console_libraries
    - --web.console.templates=/usr/share/prometheus/consoles
    - --web.enable-lifecycle
    ports:
    - ${PROMETHEUS_PORT:-12601}:9090
    volumes:
    - ./config/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    - ${PROMETHEUS_DATA_DIR:-/home/digitie/kraddr-geo-data/prometheus}:/prometheus
    depends_on:
      cadvisor:
        condition: service_started
  grafana:
    image: ${GRAFANA_IMAGE:-grafana/grafana:11.1.4}
    container_name: ${GRAFANA_CONTAINER:-tripmate-grafana}
    restart: unless-stopped
    user: ${GRAFANA_RUN_AS_USER:-0}
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
      GF_USERS_ALLOW_SIGN_UP: 'false'
      GF_SERVER_HTTP_PORT: 3000
    ports:
    - ${GRAFANA_PORT:-12605}:3000
    volumes:
    - ${GRAFANA_DATA_DIR:-/home/digitie/kraddr-geo-data/grafana}:/var/lib/grafana
    - ./config/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources:ro
    depends_on:
      prometheus:
        condition: service_started
  cadvisor:
    image: ${CADVISOR_IMAGE:-gcr.io/cadvisor/cadvisor:v0.52.1}
    container_name: ${CADVISOR_CONTAINER:-tripmate-cadvisor}
    restart: unless-stopped
    privileged: true
    devices:
    - /dev/kmsg:/dev/kmsg
    ports:
    - ${CADVISOR_PORT:-12602}:8080
    volumes:
    - /:/rootfs:ro
    - /var/run:/var/run:ro
    - /sys:/sys:ro
    - /var/lib/docker/:/var/lib/docker:ro
    - /dev/disk/:/dev/disk:ro
